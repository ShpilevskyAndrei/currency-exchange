import { FlagByCurrencyPipe } from './flag-by-currency.pipe';

describe('FlagByCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FlagByCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
